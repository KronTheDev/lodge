[package]
name = "lodge-ruleset"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
glob = "0.3"
anyhow = "1"

[dependencies.lodge-shared]
path = "../shared"
